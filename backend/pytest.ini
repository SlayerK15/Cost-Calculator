[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

[pytest:env]
# Ensure tests run in debug mode (allows insecure default secrets)
